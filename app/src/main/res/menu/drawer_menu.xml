<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <!-- Profile and Settings (These items should be in the drawer) -->
        <item
            android:id="@+id/nav_profile"
            android:title="Profile"
            android:icon="@drawable/baseline_account_circle_24" />
    </group>

    <group>
        <!-- Additional Sections (These items are in the bottom navigation) -->
        <item
            android:id="@+id/nav_schedules"
            android:title="Schedules"
            android:icon="@drawable/baseline_schedule" />
        <item
            android:id="@+id/nav_events"
            android:title="Events"
            android:icon="@drawable/baseline_event" />
        <item
            android:id="@+id/nav_news"
            android:title="News"
            android:icon="@drawable/baseline_newspaper_24" />
        <item
            android:id="@+id/nav_feedback"
            android:title="Feedback"
            android:icon="@drawable/baseline_feedback_24" />
    </group>
</menu>

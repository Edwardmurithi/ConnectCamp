<!-- res/layout/activity_welcome.xml -->
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/welcomeRelativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/backgroundColor"
    tools:context=".WelcomeActivity">

    <!-- Campus Logo -->
    <ImageView
        android:id="@+id/campusLogo"
        android:layout_width="234dp"
        android:layout_height="167dp"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="80dp"
        android:contentDescription="Campus Logo"
        android:src="@drawable/campus_logo" />

    <!-- Welcome Text -->
    <TextView
        android:id="@+id/welcomeText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/campusLogo"
        android:layout_marginTop="40dp"
        android:layout_centerHorizontal="true"
        android:text="Welcome to Campus Manager"
        android:textSize="26sp"
        android:textColor="@color/textColor"
        android:textStyle="bold" />

    <!-- Short description of the app -->
    <TextView
        android:id="@+id/welcomeDescription"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/welcomeText"
        android:layout_centerHorizontal="true"
        android:layout_marginTop="16dp"
        android:text="Manage your schedules, events, and news easily."
        android:textSize="16sp"
        android:textColor="@color/textColor"
        android:padding="16dp" />

    <!-- Welcome Button -->
    <Button
        android:id="@+id/welcomeButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="40dp"
        android:layout_marginTop="20dp"
        android:layout_below="@id/welcomeDescription"
        android:backgroundTint="@color/buttonColor"
        android:text="Explore Home"
        android:textColor="@color/inputBackground"
        android:textStyle="bold"
        android:elevation="4dp"
        android:layout_centerHorizontal="true" />

    <!-- Login Button -->
    <Button
        android:id="@+id/loginButton"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_marginStart="40dp"
        android:layout_marginEnd="40dp"
        android:layout_below="@id/welcomeButton"
        android:backgroundTint="@color/buttonColor"
        android:text="Log In"
        android:textColor="@color/inputBackground"
        android:textStyle="bold"
        android:elevation="4dp"
        android:layout_centerHorizontal="true" />




    <!-- Signup Text Link -->
    <TextView
        android:id="@+id/signupText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="30dp"
        android:layout_centerHorizontal="true"
        android:text="Don't have an account? Sign Up"
        android:textSize="16sp"
        android:textColor="@color/colorPrimary"
        android:textStyle="bold" />

</RelativeLayout>
